"""
app/db/__init__.py — package marker for the database module.

The `db/` package holds everything related to connecting to the database:
the engine (connection pool), the session factory, and the dependency
that gives each request its own database session.
"""
