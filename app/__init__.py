"""
LearnArc FastAPI Application
============================
app/__init__.py — package marker.

This empty file tells Python that the `app/` folder is a package
(a collection of related modules). Without it, Python would not be
able to do `from app.core.config import settings`.
"""
