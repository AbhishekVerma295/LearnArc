# ─── Core Framework ───────────────────────────────────────────────────────────
# FastAPI is the modern web framework we are migrating to.
# uvicorn is the ASGI server that runs FastAPI (like gunicorn for Flask).
fastapi==0.115.12
uvicorn[standard]==0.34.3

# ─── Database ─────────────────────────────────────────────────────────────────
# SQLAlchemy 2.0: the ORM that maps Python classes to database tables.
# PyMySQL: pure-Python MySQL driver (no C extensions required).
# alembic: database migration tool — tracks schema changes over time.
SQLAlchemy==2.0.41
PyMySQL==1.1.1
alembic==1.16.1

# ─── Settings ─────────────────────────────────────────────────────────────────
# pydantic-settings: reads .env files and validates environment variables.
pydantic-settings==2.9.1

# ─── Security ─────────────────────────────────────────────────────────────────
# bcrypt: password hashing (reused from the existing Flask app).
# python-jose: JWT token creation and validation (used from Phase 3 onwards).
# passlib: password hashing utilities wrapper (used with bcrypt in Phase 3).
bcrypt==4.3.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# ─── Dev Utilities ────────────────────────────────────────────────────────────
python-dotenv==1.1.0
