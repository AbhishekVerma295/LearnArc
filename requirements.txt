# ─── Core Framework ───────────────────────────────────────────────────────────
# FastAPI is the modern web framework.
# uvicorn is the ASGI server that runs FastAPI (like gunicorn for Flask).
# gunicorn is the production process manager — it spawns multiple uvicorn workers.
fastapi==0.115.12
uvicorn[standard]==0.34.3
gunicorn==23.0.0

# ─── Database ─────────────────────────────────────────────────────────────────
# SQLAlchemy 2.0: the ORM that maps Python classes to database tables.
# PyMySQL: pure-Python MySQL driver (no C extensions required).
# alembic: database migration tool — tracks schema changes over time.
SQLAlchemy==2.0.41
PyMySQL==1.1.1
alembic==1.16.1

# ─── Settings ─────────────────────────────────────────────────────────────────
# pydantic-settings: reads .env files and validates environment variables.
pydantic-settings==2.9.1

# ─── Security ─────────────────────────────────────────────────────────────────
# bcrypt: password hashing (reused from the existing Flask app).
# python-jose: JWT token creation and validation.
# passlib: password hashing utilities wrapper (used with bcrypt).
# slowapi: rate limiting for FastAPI (built on the `limits` library).
bcrypt==4.3.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
slowapi==0.1.9

# ─── Dev Utilities ────────────────────────────────────────────────────────────
python-dotenv==1.1.0

# ─── Testing ──────────────────────────────────────────────────────────────────
pytest==8.3.4
pytest-cov==6.1.0
httpx==0.28.1
python-multipart==0.0.32
